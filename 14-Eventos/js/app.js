// Evento que se ejecuta cuando se carga todo el contenido

console.log(1);

document.addEventListener('DOMContentLoaded', () => { // Cuando se carga todo el html
    console.log(2);
});

console.log(3);