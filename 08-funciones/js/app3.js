// Parametros de una funcion
function sumar(a, b) { // Parametros de la funcion
    console.log(a+b);
}

sumar(3, 5); // Argumentos de la funcion
sumar(100, 200);

function saludar(nombre, apellido) {
    console.log(`Hola ${nombre} ${apellido}`);
}

saludar('Juan', 'Perez');