const nombre = 'Juan Perez';

console.log(nombre.toUpperCase()); // Convertir a mayusculas
console.log(nombre.toLowerCase()); // Convertir a minusculas

const email = 'JPEREZ@GMAIL.COM';
console.log(email.toLowerCase());

const precio = 200;
console.log(precio);
console.log(precio.toString()); // Convertir a string